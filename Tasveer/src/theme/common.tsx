import { StyleSheet } from "react-native";

export const commonStyles = StyleSheet.create({
    rowCenter: {
        flexDirection: 'row',
        justifyContent: 'center',
        alignItems: 'center',
    },
    colCenter: {
        justifyContent: 'center',
        alignItems: 'center',
    }
})